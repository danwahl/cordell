# Cordell Environment Variables

# Required: Claude Code OAuth token (obtained via `claude setup-token`)
CLAUDE_CODE_OAUTH_TOKEN=your-oauth-token-here

# Optional: Log level (DEBUG, INFO, WARNING, ERROR)
CORDELL_LOG_LEVEL=INFO
